import React from "react";
import { shallow } from "enzyme";
import ChannelList from '../ChannelList';

describe("<ChannelList />", () => {
    //add in props for a list of channels
    // verify the right number of list items are created (ListItem from material UI)
    // verify the onClick property works via Sinon (see example in createChannelButton)
})